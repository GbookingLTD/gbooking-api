/**
 * IBusinessConfigEntry is a part of INetworkWidgetConfiguration that represents business configuration for widget
 */

export interface IBusinessConfigEntry {
  internalID: string;
  _id: string;
  active: boolean;
}
