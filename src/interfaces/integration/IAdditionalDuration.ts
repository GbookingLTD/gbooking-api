/**
 * IAdditionalDuration is a part of IProfileResponce that represents client's additional dirations field
 */

export interface IAdditionalDuration {
  level: number; // h/s/l (0, 1, 2)
  duration: number;
}
